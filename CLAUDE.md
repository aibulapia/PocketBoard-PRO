# CLAUDE.md

> 포켓보드 PRO 프로젝트에서 Claude가 작업할 때 지키는 공통 원칙.
> v2.19 작업 중 실제로 문제를 겪으며 확립됨 (2026-07-27).

## 데이터 처리 원칙

1. **구버전 데이터 호환 필수**
   저장 구조나 파싱 로직을 바꿀 때, 기존에 이미 저장된 데이터도
   그대로 동작해야 한다. 예전 형식을 판별해 별도 처리(폴백)하는
   코드를 반드시 남길 것.

2. **런타임 모호함은 안전한 쪽으로**
   코드가 실행되는 순간(조회 실패, 데이터 형식 불명확 등)에는
   "잘못된 것을 보여주기"보다 "아무것도 안 보여주거나 기존 값 유지"를
   자동으로 택한다. 사용자에게 물을 수 없는 상황이므로 6번과는 별개.

3. **엣지 케이스는 실제 업로드 파일로 검증**
   요청받은 기능은 반드시 실제 엑셀/데이터로 시뮬레이션 후
   결과를 표로 보여줄 것. 코드만 보고 "될 것 같다"고 넘어가지 않는다.

4. **한 곳을 고치면 연쇄 영향 전수 조사**
   같은 데이터/함수를 쓰는 다른 곳(grep 전수 검색)이 있는지 확인하고
   함께 고친다.

5. **완성 후 객관적 재검토를 별도로 수행**
   "다 됐습니다"로 끝내지 않고, 버그 관점에서 다시 훑어
   문제점을 먼저 보고한 뒤 결과를 전달한다.

## 소통 원칙

6. **요구사항이 모호하면 선택지를 제시**
   대화 중 요청이 애매하면 임의로 판단하지 말고,
   2~3개 선택지를 만들어 사용자가 고르게 한다.

7. **설명은 초등학생도 이해하게, 쉽고 간결하게**
   기술 용어(캐시, RPC, 커밋 등)를 쓸 때는 꼭 필요한 경우만,
   그마저도 풀어서 설명한다. 문장은 짧게.

## 기존 작업 규칙 (계속 유지)

- ⚠️ **"코드 작성" 지시 전까지는 실제 코드에 반영하지 않고 대기 목록에만 기록.**
  "반영하자", "고치자", "이렇게 하자" 같은 말만으로는 착수하지 않는다 —
  그 외에 긍정적 응답에도 임의로 판단해 코드 작성을 착수하지 않는다
  정확히 **"코드 작성"**이라는 문구가 나와야만 시작한다.
  (v2.22H 세션에서 이 규칙을 여러 번 어겨 실장님께 반복 지적받은 뒤 명문화됨)
- "코드 작성" 지시 없이는 컴파일/빌드/패키징하지 않음
- 코드 작성 전 대기 목록 전체를 다시 검토
- 코드 작성 후 제3자 관점의 객관적 재검토 수행 (원칙 5와 연결)
- 버전 규칙:
  | 변경 종류 | 처리 | 예시 |
  |---|---|---|
  | 버그 수정만 | 끝에 a, b, c… | `2.18` → `2.18a` |
  | 세부 기능·소소한 변화 | +0.01 | `2.18` → `2.19` |
  | 내용 수정·큰 변화 | +0.1 | `2.18` → `2.2` |
  | 알파벳 상태에서 기능 추가 | 알파벳 떨어짐 | `2.18a` → `2.19` |
  | 여러 종류가 섞임 | 큰 쪽 적용 + 알파벳 | `2.18` → `2.19a` |
- **README.md 버전 동기화 (2026-07-29 추가)**: 버전을 올릴 때마다
  `docs/README.md` 상단의 "버전: X.XX (날짜)"도 함께 갱신할 것.
  index.html의 APP_VERSION만 바꾸고 README를 안 고치면 문서가
  구버전에 멈춰 있는 상태가 재발한다.
- **버전 변경 시 고칠 곳 — 총 4군데**:
  1. `public/index.html`의 `APP_VERSION`
  2. `public/index.html`의 `<title>`
  3. `public/version.json`
  4. `docs/README.md` — 제목 바로 아래 `v 2.55 (26.08/27)` 형식 + 본문의 "버전: X" 줄
  > `public/sw.js`는 v2.53에서 오프라인 캐시를 제거해 `APP_VERSION` 상수가
  > 없어졌다. 더 이상 수정 대상이 아니다.
- **배포 이력 기록 필수 (2026-08-20 추가)**: 버전을 올릴 때마다 `docs/README.md`
  상단의 **"🚀 배포 이력" 표 맨 위에 한 줄 추가**할 것.
  형식: `| 버전 | 날짜(YY.MM/DD) | 주요 변경 한 줄 |`
  이력은 지우지 않고 전부 누적한다. 상세 내용은 `CHANGELOG.md`에 별도로 남길 것.
- **버전 소수점(숫자) 변경 시 사용자 컨펌 필수 (2026-08-01 추가)**: 알파벳 접미사(a,b,c…)를
  붙이는 건 그대로 진행해도 되지만, 소수점 자리(2.54 → 2.55 등)를 올릴 때는
  반드시 실장님께 먼저 확인받은 뒤 반영할 것.

## 서버 작업 원칙 (2026-08-27 추가)

이번 개편 중 실제로 겪은 문제를 바탕으로 명문화한다.

- **서버(Supabase) 작업과 파일 작업을 구분해 기록할 것.**
  작업 환경이 초기화되면 로컬 파일 변경은 사라지지만, Supabase에 적용한
  변경은 그대로 남는다. BACKLOG·CHANGELOG에 어느 쪽인지 반드시 밝힐 것.
  (실제로 문서에는 "보류"로 적혀 있는데 서버에는 이미 적용돼 있어 혼선이 있었음)
- **비밀값은 Claude가 정하지 않는다.**
  마스터 코드·백업 토큰 등은 SQL 템플릿만 만들고, 실제 값은 실장님이 직접
  채워 실행한다. Claude는 값을 알지 않으며 문서·대화에도 남기지 않는다.
- **앱과 서버를 함께 고칠 때는 순서가 있다.**
  ① 서버 SQL 먼저 적용 → ② 확인 → ③ 앱 배포.
  순서를 어기면 앱이 없는 함수를 부르게 되어 기능이 먹통이 된다.
- **서버 함수를 바꿀 때는 구버전 앱 호환을 확인할 것.**
  사용자 폰에 옛 앱이 열려 있을 수 있다. 인자를 추가할 때는 기본값을 주고,
  기존 호출 방식이 그대로 동작하는지 실제로 테스트한다.

## 문서 구조 (2026-08-27 개편)

| 문서 | 대상 | 용도 |
|---|---|---|
| `docs/README.md` | 개발자 | 폴더 구조, 설정 방법, **배포 이력 표** |
| `docs/CHANGELOG.md` | 개발자 | 버전별 상세 변경 이력 |
| `docs/BACKLOG.md` | 개발자 | 할 일·보류 목록 |
| `docs/포켓보드PRO_백서.md` | 상급자·인수인계 | 사람이 읽는 소개 문서 |
| `docs/포켓보드PRO_개발문서.md` | 개발자·AI | **아키텍처 + 기술사양 + 재구현 가이드** |
| `docs/포켓보드PRO_사용설명서.html` | 감독자 | 현장 사용법 |
| `docs/archive/` | — | 낡은 문서 보관 (참고용, 갱신 안 함) |

- 큰 개편을 하면 **백서와 개발문서도 함께 갱신**할 것.
  (기능은 계속 바뀌는데 백서가 v2.24b에 멈춰 있어 공장 수·오프라인 지원 등이
  사실과 달랐던 사례가 있었다)
