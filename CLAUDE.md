# CLAUDE.md

> 포켓보드 PRO 프로젝트에서 Claude가 작업할 때 지키는 공통 원칙.
> v2.19 작업 중 실제로 문제를 겪으며 확립됨 (2026-07-27).

## 데이터 처리 원칙

1. **구버전 데이터 호환 필수**
   저장 구조나 파싱 로직을 바꿀 때, 기존에 이미 저장된 데이터도
   그대로 동작해야 한다. 예전 형식을 판별해 별도 처리(폴백)하는
   코드를 반드시 남길 것.

2. **런타임 모호함은 안전한 쪽으로**
   코드가 실행되는 순간(조회 실패, 데이터 형식 불명확 등)에는
   "잘못된 것을 보여주기"보다 "아무것도 안 보여주거나 기존 값 유지"를
   자동으로 택한다. 사용자에게 물을 수 없는 상황이므로 6번과는 별개.

3. **엣지 케이스는 실제 업로드 파일로 검증**
   요청받은 기능은 반드시 실제 엑셀/데이터로 시뮬레이션 후
   결과를 표로 보여줄 것. 코드만 보고 "될 것 같다"고 넘어가지 않는다.

4. **한 곳을 고치면 연쇄 영향 전수 조사**
   같은 데이터/함수를 쓰는 다른 곳(grep 전수 검색)이 있는지 확인하고
   함께 고친다.

5. **완성 후 객관적 재검토를 별도로 수행**
   "다 됐습니다"로 끝내지 않고, 버그 관점에서 다시 훑어
   문제점을 먼저 보고한 뒤 결과를 전달한다.

## 소통 원칙

6. **요구사항이 모호하면 선택지를 제시**
   대화 중 요청이 애매하면 임의로 판단하지 말고,
   2~3개 선택지를 만들어 사용자가 고르게 한다.

7. **설명은 초등학생도 이해하게, 쉽고 간결하게**
   기술 용어(캐시, RPC, 커밋 등)를 쓸 때는 꼭 필요한 경우만,
   그마저도 풀어서 설명한다. 문장은 짧게.

## 기존 작업 규칙 (계속 유지)

- ⚠️ **"코드 작성" 지시 전까지는 실제 코드에 반영하지 않고 대기 목록에만 기록.**
  "반영하자", "고치자", "이렇게 하자" 같은 말만으로는 착수하지 않는다 —
  정확히 **"코드 작성"**이라는 문구가 나와야만 시작한다.
  (v2.22H 세션에서 이 규칙을 여러 번 어겨 실장님께 반복 지적받은 뒤 명문화됨)
- "코드 작성" 지시 없이는 컴파일/빌드/패키징하지 않음
- 코드 작성 전 대기 목록 전체를 다시 검토
- 코드 작성 후 제3자 관점의 객관적 재검토 수행 (원칙 5와 연결)
- 버전 규칙:
  | 변경 종류 | 처리 | 예시 |
  |---|---|---|
  | 버그 수정만 | 끝에 a, b, c… | `2.18` → `2.18a` |
  | 세부 기능·소소한 변화 | +0.01 | `2.18` → `2.19` |
  | 내용 수정·큰 변화 | +0.1 | `2.18` → `2.2` |
  | 알파벳 상태에서 기능 추가 | 알파벳 떨어짐 | `2.18a` → `2.19` |
  | 여러 종류가 섞임 | 큰 쪽 적용 + 알파벳 | `2.18` → `2.19a` |
- **README.md 버전 동기화 (2026-07-29 추가)**: 버전을 올릴 때마다
  `docs/README.md` 상단의 "버전: X.XX (날짜)"도 함께 갱신할 것.
  index.html의 APP_VERSION만 바꾸고 README를 안 고치면 문서가
  구버전에 멈춰 있는 상태(v2.5 방치 사례)가 재발한다.
- **sw.js 버전 동기화 (2026-08-08 추가, v2.5c부터)**: 버전을 올릴 때
  `public/sw.js` 맨 위의 `APP_VERSION`도 **반드시 같은 값으로** 바꿀 것.
  이 값이 서비스워커 캐시 이름이라, 안 바꾸면 사용자 폰이 **옛 화면을 계속 캐시에서
  꺼내 써서 새 배포가 반영되지 않는다.** 버전을 바꿔야 새 캐시로 교체되고 옛 캐시가
  자동 삭제된다. 즉 버전 변경 시 고칠 곳은 총 4군데:
  `index.html`의 APP_VERSION·`<title>`, `public/sw.js`의 APP_VERSION, `docs/README.md`.
- **버전 소수점(숫자) 변경 시 사용자 컨펌 필수 (2026-08-01 추가)**: 알파벳 접미사(a,b,c…)를
  붙이는 건 그대로 진행해도 되지만, 소수점 자리(2.22 → 2.23 등)를 올릴 때는
  반드시 실장님께 먼저 확인받은 뒤 반영할 것.

## 데이터 구조상 반드시 기억할 함정 (2026-08-10, v2.5g 추가)

- **병합 저장은 "삭제"를 표현하지 못한다.**
  `storage.js`의 저장은 서버 목록과 내 목록을 **카드별로 합쳐** 더 최신인 쪽을 남긴다.
  이 방식은 "고친 것"은 정확히 반영하지만, **"없앤 것"은 표현할 수단이 없다.**
  내 목록에서 뺀 카드는 서버에 남아 있으면 그대로 되살아난다.
  → v2.19~v2.5f의 "최종일 지난 카드 자동 삭제"가 이 함정에 걸려
    삭제 → 부활 → 재삭제가 무한 반복됐고, 그 사이 조회가 한 번 실패하면
    목록 전체가 서버에서 사라지는 구조였다(실제로 데이터가 비워진 흔적 확인됨).
  - **앞으로 삭제 성격의 기능을 넣을 때는 반드시 다음 중 하나를 함께 설계할 것:**
    ① 저장 직전에도 같은 삭제 규칙을 한 번 더 적용한다 (v2.5g가 택한 방식,
       `MMEECStorage.setPostMergeFilter`). 단, **모든 기기가 같은 결론에 도달하는
       결정론적 규칙**(날짜 비교 등)일 때만 쓸 수 있다.
    ② 지운 카드에 "지움 표시"를 남겨 서버와 함께 비교한다(묘비 방식).
       기기마다 판단이 갈리는 삭제(개별 카드 삭제 등)에는 이 방식이 필요하다.
  - ③ "목록 통째로 덮어쓰기"는 v2.5d에서 남의 기록이 지워지는 사고를 낸 방식이므로
    되돌아가지 말 것.

- **"서버가 비어 있음"과 "조회 실패"는 반드시 구분한다.**
  v2.5f까지는 서버 목록이 0건이면 실패로 간주해 폰의 옛 목록을 썼다.
  그래서 초기화·시즌 종료로 서버를 비워도 다른 기기에는 옛 목록이 그대로 보였고,
  그 기기가 저장하는 순간 옛 데이터 전체가 되살아났다.
  저장소 행 자체가 없을 때(신규)만 폰 데이터로 폴백할 것.

- **초기화 시 전송 대기열(`MMEEC_PENDING_SAVE`)도 함께 비운다.**
  다른 기기의 대기 기록은 초기화 시각과 비교해, 초기화보다 이전 것이면 버린다.

- **자동백업은 살아 있다고 가정하지 말 것.**
  `session_registry`가 비어 있으면 백업 함수는 매일 돌아도 아무것도 내보내지 않는다.
  삭제 성격의 기능을 설계할 때는 백업이 실제로 도는지 먼저 확인할 것.

- **테스트 시 시간대 주의.** 06시 하루 전환 로직이 있으므로 검증은 반드시
  `TZ=Asia/Seoul`로 실행한다. UTC로 돌리면 하루가 어긋나 결과가 뒤집힌다.
